import styled from 'styled-components';

const Container = styled.div`
  max-width: 1170px;
  margin: auto;
`;

const Header = styled.header``;

const ItemContainer = styled.ul`
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
`;

export { Container, Header, ItemContainer };
